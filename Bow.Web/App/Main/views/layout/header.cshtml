@*<div class="navbar navbar-inverse navbar-fixed-top" role="navigation" ng-controller="app.views.layout.header as vm">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#/"><img class="logo" src="~/App/Main/images/logo-tipografia.png" alt="Logo Bow"></a>
        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li class="dropdown" ng-repeat="menuItem in vm.menu.items" ng-class="{active: vm.currentMenuName == menuItem.name}">
                    <a ng-href="{{menuItem.url}}" data-toggle="dropdown"><i class="{{menuItem.icon}}" ng-if="menuItem.icon"></i> {{menuItem.displayName}} <b class="caret"></b> </a>
                    <ul class="dropdown-menu">
                        <li ng-repeat="ddMenu in menuItem.items">
                            <a ng-href="{{ddMenu.url}}">{{ddMenu.displayName}}</a>
                        </li>
                    </ul>
                </li>
            </ul>
            
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a id="drop-avatar" class="dropdown-toggle" aria-expanded="false" aria-haspopup="true" data-toggle="dropdown" href="#">
                        <img src="~/App/Main/images/usuarios/hp.jpg" class="img-circle img-responsive img-avatar-menu right-menu" alt="Hozkar Patrick Llano Restrepo" />
                        <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu" aria-labelled-by="drop-avatar" role="menu">
                        <li role="presentation">
                            Hora de entrada <br> 9:46 a.m
                        </li>
                        <li ng-repeat="language in vm.languages" ng-hide="vm.currentLanguage.name == language.name">
                            <a href="/AbpLocalization/ChangeCulture?cultureName={{language.name}}"><i ng-class="language.icon"></i> {{language.displayName}}</a>
                        </li>
                        <li role="presentation">
                            <a href="#" tabindex="-1" role="menuitem"> Cerrar Sesión </a>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</div>*@
<!-- START Top Navbar-->
<nav role="navigation" class="navbar topnavbar">
    <!-- START button offset-->
    <div class="btn-offset">
        <button type="button" btn-layer-morph="" target="#layer-search" ripple="" class="btn btn-info btn-circle btn-lg shadow-z1">
            <em class="fa fa-search"></em>
        </button>
    </div>
    <!-- START button offset-->
    <!-- START navbar header-->
    <div class="navbar-header">
        <a ui-sref="home" class="navbar-brand">
            <img src="~/App/Main/images/logo-tipografia-sm.png" alt="App Logo" class="brand-logo" />
        </a>
        <!-- Mobile buttons-->
        <div class="mobile-toggles">
            <!-- Button to show/hide the sidebar on mobile. Visible on mobile only.-->
            <a href="" ng-click="app.sidebar.isOffscreen = !app.sidebar.isOffscreen" class="sidebar-toggle">
                <em class="fa fa-navicon"></em>
            </a>
            <!-- Button to show/hide the header menu on mobile. Visible on mobile only.-->
            <a href="" ng-click="toggleHeaderMenu()" class="menu-toggle hidden-material">
                <em class="fa fa-ellipsis-v fa-fw"></em>
            </a>
        </div>
    </div>
    <!-- END navbar header-->
    <!-- START Nav wrapper-->
    <div collapse="headerMenuCollapsed" class="nav-wrapper collapse navbar-collapse">
        <!-- START Left navbar-->
        <ul class="nav navbar-nav hidden-material">
            <li>
                <!-- Button used to collapse the left sidebar. Only visible on tablet and desktops-->
                <a href="" ng-click="app.sidebar.isOffscreen = !app.sidebar.isOffscreen" class="hidden-xs">
                    <em ng-class="app.sidebar.isOffscreen ? 'fa-caret-right':'fa-caret-left'" class="fa"></em>
                </a>
            </li>
            <li>
                <a href="@Url.Action("Logout", "Account")" title="Lock screen" ripple="">
                    <em class="fa fa-sign-out"></em>
                    <span class="visible-xs-inline ml">Lock Screen</span>
                    <span class="ripple"></span>
                </a>
            </li>
        </ul>
        <!-- END Left navbar-->
    </div>
    <!-- END Nav wrapper-->
</nav>
<!-- END Top Navbar-->